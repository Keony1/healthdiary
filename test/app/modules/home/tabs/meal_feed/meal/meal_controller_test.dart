import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:healthdiary/app/modules/home/tabs/meal_feed/meal/meal_controller.dart';
import 'package:healthdiary/app/modules/home/tabs/meal_feed/meal_feed_module.dart';

void main() {
  initModule(MealFeedModule());
  // MealController meal;
  //
  setUp(() {
    //     meal = MealFeedModule.to.get<MealController>();
  });

  group('MealController Test', () {
    //   test("First Test", () {
    //     expect(meal, isInstanceOf<MealController>());
    //   });

    //   test("Set Value", () {
    //     expect(meal.value, equals(0));
    //     meal.increment();
    //     expect(meal.value, equals(1));
    //   });
  });
}
