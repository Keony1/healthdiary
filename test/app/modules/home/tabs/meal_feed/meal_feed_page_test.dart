import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:healthdiary/app/modules/home/tabs/meal_feed/meal_feed_page.dart';

main() {
  testWidgets('MealFeedPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MealFeedPage(title: 'MealFeed')));
    //  final titleFinder = find.text('MealFeed');
    //  expect(titleFinder, findsOneWidget);
  });
}
