import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:healthdiary/app/modules/home/tabs/meal_feed/meal_feed_module.dart';

void main() {
  initModule(MealFeedModule());
  // MealFeedController mealfeed;
  //
  setUp(() {
    //     mealfeed = MealFeedModule.to.get<MealFeedController>();
  });

  group('MealFeedController Test', () {
    //   test("First Test", () {
    //     expect(mealfeed, isInstanceOf<MealFeedController>());
    //   });

    //   test("Set Value", () {
    //     expect(mealfeed.value, equals(0));
    //     mealfeed.increment();
    //     expect(mealfeed.value, equals(1));
    //   });
  });
}
