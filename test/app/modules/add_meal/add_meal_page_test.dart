import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:healthdiary/app/modules/add_meal/add_meal_page.dart';

main() {
  testWidgets('AddMealPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddMealPage(title: 'AddMeal')));
    //  final titleFinder = find.text('AddMeal');
    //  expect(titleFinder, findsOneWidget);
  });
}
