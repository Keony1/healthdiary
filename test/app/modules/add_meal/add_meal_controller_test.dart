import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:healthdiary/app/modules/add_meal/add_meal_controller.dart';
import 'package:healthdiary/app/modules/add_meal/add_meal_module.dart';

void main() {
  initModule(AddMealModule());
  // AddMealController addmeal;
  //
  setUp(() {
    //     addmeal = AddMealModule.to.get<AddMealController>();
  });

  group('AddMealController Test', () {
    //   test("First Test", () {
    //     expect(addmeal, isInstanceOf<AddMealController>());
    //   });

    //   test("Set Value", () {
    //     expect(addmeal.value, equals(0));
    //     addmeal.increment();
    //     expect(addmeal.value, equals(1));
    //   });
  });
}
